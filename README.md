# Project Structure

Each task related to a specific session is organized in its own separate folder.

If needed, a `README.md` file is created inside each folder to provide detailed information about that task.
